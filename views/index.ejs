<%- include('layout/header') %>

<div class="container">
    <div class=" row">
        <div class="col-lg-12">
            <% if (message) { %>
             <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
                <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                <strong><%= message.message %></strong>
             </div>
             <% } %>
            <div class="table-responsive my-3">
                <% if (tickets != '') { %>
                    <table class="table table-stripped text-center my-3">
                        <thead>
                        <tr class="table-dark">
                            <th>Ticket ID</th>
                            <th>User ID</th>
                            <th>E-Mail</th>
                            <th>Phone</th>
                            <th>Address</th>
                            <th>Description</th>
                            <th>Image</th>
                            <th>Action</th>
                        </tr>
                        </thead>
                        <tbody>
                            <% tickets.forEach((row, index) => { %>
                                <tr class="align-middle">
                                    <td><%= index %></td>
                                    <td><%= row.user_id %></td>
                                    <td><%= row.email %></td>
                                    <td><%= row.phone %></td>
                                    <td><%= row.address %></td>
                                    <td><%= row.description %></td>
                                    <td><img id="img" src="<%= row.image %>" width="80" class="img-thumbnail" /></td>
                                    <td>
                                      <a href="/edit/<%= row._id %>" class="text-success"><i class="fa-solid fa-pen-to-square"></i></a>
                                      <a href="/delete/<%= row._id %>" class="text-danger"><i class="fa-solid fa-trash"></i></a>
                                    </td>
                                </tr>                            
                            <% }) %> 
                        </tbody>
                    </table>
                <% } else { %>
                    <h1 class="text-center text-secondary mt-5">No tickets found in the database!</h1>
                <% } %>
            </div>
        </div>
    </div>
</div>
<div id="myModal" class="modal">
    <span class="modal-close">&times;</span>
    <img class="modal-content" id="img01">
</div>

<%- include('layout/footer') %>
